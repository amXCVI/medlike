import 'package:flutter/material.dart';

class HealthValue extends StatelessWidget {
  const HealthValue({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
