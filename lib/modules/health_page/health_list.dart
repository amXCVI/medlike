import 'package:flutter/material.dart';

class HealthList extends StatelessWidget {
  const HealthList({Key? key}) : super(key: key);
  //'assets/icons/indicators/{}'

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
