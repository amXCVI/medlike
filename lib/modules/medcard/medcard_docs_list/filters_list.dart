import 'package:flutter/material.dart';

class MedcardFiltersList extends StatelessWidget {
  const MedcardFiltersList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
