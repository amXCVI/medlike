import 'package:flutter/material.dart';

class MedcardList extends StatelessWidget {
  const MedcardList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
